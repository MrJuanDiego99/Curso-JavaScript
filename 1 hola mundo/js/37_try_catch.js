//Estructura try catch


try { //Todo lo que sea suceptible a fallos
    var year = 2022;
    alert(yea);
} catch (error) { //Capturamos el error e informamos 
    alert("a ocurrido un error: "+ error)
}